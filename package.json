{
  "name": "tools-vanshul",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Private, offline developer toolbox — tools.vanshul.com. The shipped app (index.html, lib.js, sw.js) has zero runtime dependencies; the packages here are dev-only for the test suite.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@vitest/coverage-v8": "^3.2.7",
    "vitest": "^3.2.7"
  }
}
